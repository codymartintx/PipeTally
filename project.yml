name: PipeTally
options:
  minimumXcodeGenVersion: 2.42.0
  deploymentTarget:
    iOS: "26.0"
settings:
  base:
    SWIFT_VERSION: 6.0
    IPHONEOS_DEPLOYMENT_TARGET: "26.0"
targets:
  PipeTally:
    type: application
    platform: iOS
    deploymentTarget: "26.0"
    sources:
      - path: PipeTally
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.codymartin.PipeTally
        PRODUCT_NAME: PipeTally
        INFOPLIST_FILE: PipeTally/Info.plist
        TARGETED_DEVICE_FAMILY: "1"
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: WBQM995SSV
